package class14Methods;

public class Methods3 {

    //write a method that takes 2 integers, add them and print it
    void add(int x, int y){
        System.out.println(x + y);
    }

    //instead of 'void' we can use int so that we can store
    //what we are returning with this method
    int sub (int n1, int n2){
        return n1-n2;
    }
}
